package com.everis.demo.utlis;

/*@Getter
@Setter
@AllArgsConstructor
@NoArgsConstructor
@EqualsAndHashCode(onlyExplicitlyIncluded = true)
@Entity
@Table(name = "t_profile_operation")*/
public class ProfileOperation {

	/*
	 * @Id
	 * 
	 * @GeneratedValue(strategy = GenerationType.IDENTITY)
	 * 
	 * @EqualsAndHashCode.Include
	 * 
	 * @Column(name = "dealer_id") private Long id;
	 * 
	 * @Column(name = "profile_id")
	 * 
	 * @ManyToOne
	 * 
	 * @JoinColumn(name = "profile_id") private Profile profile;
	 * 
	 * @Column(name = "operation_type_id")
	 * 
	 * @ManyToOne
	 * 
	 * @JoinColumn(name = "operation_type_id") private UserOperationType
	 * userOperationType;
	 */
}
